#Entorno
DEBUG=True
SECRET_KEY="KEY DE TRABAJANDO DE DJANGO"
DATABASE_URL="RUTA PARA CONEXIÓN A BASE DE DATOS"
ALLOWED_HOSTS=localhost,127.0.0.1
RECAPTCHA_SECRET_KEY="CAPTCHA KEY"

# Redis para cache y códigos temporales
REDIS_URL=redis://localhost:6379/0

# Email / SMTP 
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=cupidoup1@gmail.com
EMAIL_HOST_PASSWORD=nzboyoqvawpbhcew
DEFAULT_FROM_EMAIL=cupidoup1@gmail.com

FRONTEND_URL=http://localhost:8080/

# CORS (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:8080

# Production security overrides (solo cuando DEBUG=False, al parecer estan en settings.py)
#SECURE_HSTS_SECONDS=31536000
#SECURE_HSTS_INCLUDE_SUBDOMAINS=True
#SECURE_HSTS_PRELOAD=False
#SECURE_SSL_REDIRECT=True
#CSRF_TRUSTED_ORIGINS=

# DB connection tweaks (opcional,ya esta en settings.py)
# Throttles (opcional, ya esta en settings.py)
# JWT settings (opcional, ya esta en settings.py)
# Logging(ya esta en settings.py)